Reply :
 - checker si il y a des RPL pour les commandes non set up
 - ajouter server.getCreationDate()
 - ajouter manuellement userModes, channelModes pour RPL004

Commandes utilisees pour les RPL et ERR :

    - NICK - ajouter RPL + verifier que ok si nick deja pris + checker RPL sur IRSSI quand Server sera ok  
    - MOTD - ok -> regler params vide sur Handle command + irssi
    - OPER
    - PONG
    - PING
    - QUIT
    - USER - ok -> verifier les histoires de MODES (do we use it) + checker RPL sur IRSSI quand Server sera ok

    On les fait ?
    - PASS
    - WHO
    - WHOIS
    - WHOWAS

    CHANNEL CMDS :
    - JOIN
    - LIST
    - NAMES
    - NOTICE
    - PART
    - PRIVMSG
    - TOPIC


Jacinthe :
    - gerer les doublons dans NICK
    - interdire NICK sans params

A faire JAcinthe :
penser a bien verifier quand on quitte avec un user de supprimer de la liste des usernick pour pas bloquer!
regarder au niveau des sorties pour egager tout proprement.

TO DO VACS :
- LEAKS a trouver

- QUIT dresser un protocole détaillé de ce qu'on doit faire en quittant

- removeUser() pouvoir lui passer le FD du User en paramètre

- OPER ajouter la fonction KILL?
(si oui adapter error message de JOIN)

-Réparer Channel sur IRSSI
   -NAMES
   -MODE
   -PART
   -JOIN
   - PRVMSG
   - (NOTICE)


   to do 27 fevrier :
    - changer le mdp
    - pb pour set le nick en premier sur nc.
    - nick sas rien sur nc doit afficher le nick., et la ce nest pas le cas.
    -join marche pas sur nc-quit non plus;
    on peut set les meme mdp en passant par -n.
    - voir quand on se reco. 